# SOLARIS-02: serialized exact-commit Solaris qualification runner (in progress)

Base: `4060ab0` (`work/SOLARIS-01` tip), an explicit dependency-base
override per the coordinator's own instruction -- SOLARIS-01 is frozen
for independent review and not yet merged to `main`, so this branch is
based directly on its tip rather than waiting for a `main` merge.
Branch pushed early to claim `work/SOLARIS-02` per `AGENTS.md`'s
race-avoidance convention.
