# IO-01: Descriptor Readiness Polling

## Goal
Implement the core non-blocking readiness detection interface (`poll`) across all supported descriptor types (pipes, files, and console).

## Implementation
Following the decisions established in `IO-01-design`, the `poll` API allows tasks to synchronously evaluate or safely block on descriptor availability.

Key design points correctly integrated into the engine:
- **API and Header Exposure:** Created `<poll.h>` mapping `poll` and `struct pollfd` structures using the `cb_libc` binding.
- **Backwards Compatibility:** Adjusted the `api_is_usable` linkage validation logic to correctly treat a `struct cb_api_v1` that shrinks exactly to the pre-`poll` boundaries (`offsetof(..., poll)`) as a fully compliant, older-versioned table. Attempts to call `poll` on this degraded mock API cleanly return `-1` with task errno set to `CB_ENOSYS`.
- **Event Scoping:** Adjusted the internal pipe-read logic to properly mask readiness. Unconditional signals (`CB_POLL_HUP`, `CB_POLL_ERR`) are strictly reported even if `CB_POLLIN` / `CB_POLLOUT` aren't requested, satisfying proper edge triggers on teardowns.
- **Clock Dependency and Timeouts:**
  - Tasks specifying an elapsed deadline (`timeout > 0`) measure their precise boundary using the host's monotonic `monotonic_millis()` interface upon system call entry.
  - If the host environment does not possess a monotonic clock (returning `0` on entry or mid-wait), the kernel aggressively aborts the loop, explicitly returning `-1` with task errno set to `CB_ENOSYS` to avoid silently mutating finite demands into infinite locks.
  - Standard zero-value readiness and indefinite `-1` requests natively ignore the clock safely.
- **Scheduler Interleaves:** Re-configured `cb_kernel_run`'s idle loop. Tasks blocked on `CB_TASK_BLOCKED_POLL` are tracked to compute the minimum global wake timeout. The scheduler dynamically delegates this precise bounds to `host->console_poll(...)`, seamlessly ensuring timely wakeups without indefinite spins or CPU burning.

All explicit exact-head CI requirements were executed with memory leak checks passing cleanly.


**Corrections applied post-review:**
- Removed misleading red/green claims regarding `poll(NULL)` and shell `&` execution. Replaced with genuine `api->spawn` and native test structures using `api->yield` directly to guarantee deterministic preemption.
- Corrected `get_poll_timeout` clock loss detection to verify loss specifically during `console_poll` sweeps and appropriately degrade to ENOSYS immediately.
- Preserved core kernel errnos (e.g., `CB_EINVAL` on NULL pipes) through `cb_libc_pipe` without double-negation or clobbering to `EPERM`.

## Integration with ERR-01 and CI-01

The integration retains every prior test registration, the shared Mac acceptance
suite, and `err(3)`. `poll` is the only appended API field; the old API prefix is
unchanged. The libc startup guard uses `offsetof(struct cb_api_v1, poll)`, while
calling an absent poll capability returns `-1` with task errno `CB_ENOSYS`.
Removed two unregistered ordinary-source pollwake placeholders; the registered
core spawn/yield wakeup tests remain. The poll source object now declares its
actual header dependencies, and `--poll` selects the focused polling tests.

The combined `build/test_core --poll`, `build/test_core --err`, and full
`make LDLIBS=-lucontext SANITIZE_CC=clang ci` passed in the Linux agent image.
Exact integration Woodpecker and Mac guest acceptance remain pending at this
commit; the coordinator owns the guest slot.
